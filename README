INSTALLING I3
=============

1) install i3 and git
$ sudo apt install i3 git

2) clone this repo
$ git clone https://github.com/dasty/i3-config.git ~/.i3

3) log out and login again using i3

FINISHING INSTALLATION
======================

1) install extra needed packages
$ sudo apt install xautolock vim undistract-me

2) add following lines to .bashrc

cat << '-----EOF-----' >> ~/.bashrc

# inspired from https://faq.i3wm.org/question/2498/ssh-sessions-in-i3.1.html
if [ "$DESKTOP_SESSION" = "i3" ]; then
    export $(gnome-keyring-daemon -s)
fi

export EDITOR=vim

# get notification when longer command finishes
source /etc/profile.d/undistract-me.sh
-----EOF-----

3) set different theme
$ sudo apt install gnome-tweaks
$ gnome-tweaks
  * set Appearance => Themes => Applications => Radiance

TIPS
====

XDG_CURRENT_DESKTOP=GNOME gnome-control-center
